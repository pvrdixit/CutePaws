import Foundation

/// Namespace for discover-feature use cases (interactors) that compose repositories.
/// Add types here as the presentation layer sheds direct repository calls.
enum DiscoverUsecases {}
